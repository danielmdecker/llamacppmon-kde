{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "github": {
      "type": "local",
      "command": [
        "sh",
        "-c",
        "docker run -i --rm -e GITHUB_PERSONAL_ACCESS_TOKEN=$(gh auth token) -e GITHUB_TOOLSETS ghcr.io/github/github-mcp-server stdio"
      ],
      "environment": {
        "GITHUB_TOOLSETS": "repos,issues,pull_requests"
      },
      "enabled": true
    }
  }
}
